/**
 * Install script for openclaw-honcho (Runtime fork)
 *
 * This fork deliberately does NOT migrate or archive workspace files.
 * Local memory-core stays untouched. Honcho runs as a supplementary layer.
 */

console.log("openclaw-honcho (Runtime fork): No migration needed — running as supplementary memory layer.");
console.log("✓ Plugin installed successfully!");
